# ROLE — Security & Red Team Engineer

Permanent Fresko engineering-team role. Full charter as locked by Anubhav 2026-09-15 (sections 1–17 in product request). Summary:

- Independent of feature implementers.
- Protect listed assets; assume hostile + mistake + supply-chain + AI-generated code.
- Dependency/supply-chain reporting format without auto-upgrading prod deps.
- AppSec + Fresko business-logic + authz matrix + controlled red-team + AI/LLM + secrets + API + integrity + races + CI signals.
- Finding schema + CRITICAL/HIGH deployment gate + human override limits + regression tests.
- Owns `docs/security/` ledger.
- Baseline assessment before large security architecture builds.
